# Project exclude paths
/.gradle/
# Cualquier directorio `build`, a CUALQUIER nivel. Antes iban listados módulo por módulo
# (/build/, /app/build/, /core/build/) y eso obliga a acordarse al añadir uno: `:baselineprofile`
# entró el 9 ago 2026 y su carpeta de build quedó sin ignorar, apareciendo como cambio pendiente.
build/
/.kotlin/
*.logcat
/.claude
/.idea
claude.md
limpieza.md
logs.txt
artefactos.jpg
captura.jpeg
/docs

# Claves de firma y configuración local (NUNCA commitear)
*.jks
*.keystore
keystore.properties
signing.properties
local.properties

# Capturas del profiler (Perfetto). Pesan cientos de MB y son de una sesión concreta.
/performance/
